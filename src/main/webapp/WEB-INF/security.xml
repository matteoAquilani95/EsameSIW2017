<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.2.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

	<global-method-security pre-post-annotations="enabled"/>

	<http use-expressions="true">
		<intercept-url pattern="/users**" access="hasRole('ROLE_ADMIN')"/>
		<intercept-url pattern="/users/**" access="hasRole('ROLE_ADMIN')"/>
		<intercept-url pattern="/artist**" access="hasRole('ROLE_USER')"/>
		<intercept-url pattern="/artist/**" access="hasRole('ROLE_USER')"/>
		<intercept-url pattern="/artists**" access="hasRole('ROLE_USER')"/>
		<intercept-url pattern="/artists/**" access="hasRole('ROLE_USER')"/>
		<intercept-url pattern="/artists/remove/**" access="hasRole('ROLE_ADMIN')"/>
		<intercept-url pattern="/edit/**" access="hasRole('ROLE_ADMIN')"/>
		<intercept-url pattern="/picture/remove/**" access="hasRole('ROLE_ADMIN')"/>
		<intercept-url pattern="/account**" access="hasRole('ROLE_USER')"/>
		
		<form-login login-page="/login"
				authentication-failure-url="/login?error=true"
				username-parameter="email"
				password-parameter="password"/>
		
		<logout logout-url="/logout"/>
	</http>
	
	<authentication-manager>
		<authentication-provider>
			
			<password-encoder hash="bcrypt"/>
			
			<jdbc-user-service data-source-ref="dataSource"
				authorities-by-username-query="select app_user.email, role.name from app_user
											   join app_user_role on app_user.id = app_user_role.users_id
											   join role on app_user_role.roles_id = role.id
											   where app_user.email = ?"
				users-by-username-query="select email,password,enabled from app_user where email = ?" />
			
		</authentication-provider>
</authentication-manager>

</beans:beans>
